Installation steps to run server locally

Open a terminal
Clone the project from the git clone link ( Command : git clone url)
Go into project directory.
Run yarn
Create a copy of .env.example and rename it to .env
Run nodemon to start the server
After Starting server go to http://localhost:5000 to run apollo server and run all the mutations to verify functionality
That's it